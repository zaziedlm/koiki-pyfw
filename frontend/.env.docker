# React/Vite frontend configuration for Docker Compose.
# Browser requests remain same-origin and nginx/Vite proxy /api to FastAPI.
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://app:8000

# nginx runtime upstream (used by the production/optimized image).
API_UPSTREAM=http://app:8000
