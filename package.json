{
  "name": "koiki-pyfw-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Root-level developer tooling only. The application backend is Python (pyproject.toml); the frontend has its own package.json under frontend/.",
  "devDependencies": {
    "@fission-ai/openspec": "1.6.0"
  }
}
