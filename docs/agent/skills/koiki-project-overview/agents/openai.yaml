interface:
  display_name: "KOIKI Project Overview"
  short_description: "Route work to the right KOIKI layer, including apps/ and frontend contract boundaries."
  default_prompt: "Use $koiki-project-overview to classify this KOIKI task, check API ownership and frontend contract impact, and identify the right code area first."
