interface:
  display_name: "KOIKI Business App Feature Work"
  short_description: "Implement downstream business-app backend changes and assess frontend contract impact."
  default_prompt: "Use $koiki-business-app-feature-work to implement this downstream backend feature under apps/, composing components/ without editing them, and assess any root frontend contract impact."
