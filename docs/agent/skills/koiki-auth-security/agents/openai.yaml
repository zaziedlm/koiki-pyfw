interface:
  display_name: "KOIKI Auth and Security"
  short_description: "Handle auth, RBAC, SSO, and SAML safely."
  default_prompt: "Use $koiki-auth-security to review and implement this KOIKI auth or security-sensitive change."
