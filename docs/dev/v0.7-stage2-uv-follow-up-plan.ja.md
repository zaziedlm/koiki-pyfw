# KOIKI-FW v0.7 Stage 2 `uv` follow-up plan

## 目的

Stage 2 の `uv` 実移行により、root workspace、`uv.lock`、local docs、CI の標準経路は `uv` に切り替わった。

一方で、Docker、security audit runbook、古い helper、component package metadata、履歴文書には Poetry 前提の記述や設定が残っている。

この文書は、残存 Poetry 要素を次工程で段階的に整理するための follow-up 計画である。

## 現在の基準

現行の標準経路:

- 依存同期: `uv sync`
- lockfile 固定同期: `uv sync --locked`
- テスト実行: `uv run pytest ...`
- CI 実行: `uv sync --locked` と `uv run --locked ...`
- lockfile 正本: `uv.lock`

現行の非標準経路:

- `poetry.lock`
- `poetry install`
- `poetry run ...`
- `poetry build`
- `[tool.poetry]`
- `poetry-core`

## 分類ルール

Poetry 残存は次の3種類に分類する。

### A. 実運用残存

現在も人、CI、Docker、script が実行し得る Poetry 前提。

原則として follow-up で更新または削除する。

例:

- Dockerfile
- security audit command
- test guide
- SAML setup guide
- helper script
- package metadata

### B. component metadata 残存

component package を build / install するための `pyproject.toml` に残る Poetry 固有設定。

`pyproject.toml` 自体は削除しない。
Poetry 固有部分だけを PEP 517 / PEP 621 の別 backend へ置き換える。

### C. 履歴文書

過去の設計、過去の release note、移行経緯を説明する文書。

原則として内容を書き換えない。
必要な場合だけ、冒頭に historical reference であることと、現行手順へのリンクを追記する。

## Follow-up タスク

### FU-1: Docker / Compose の `uv` 化

優先度:

- 最優先

理由:

- `poetry.lock` は削除済みであり、Dockerfile が `COPY poetry.lock` や `poetry install` を参照している場合、Docker build が破綻する。

対象:

- `Dockerfile`
- `Dockerfile.unified`
- `docker-compose*.yml`
- `scripts/verify-dockerfile-unified.sh`
- `scripts/verify-dockerfile-unified.ps1`

作業:

- Poetry install step を削除する
- `uv` install step を追加する
- `COPY poetry.lock` を `COPY uv.lock` に変更する
- `poetry install` を `uv sync --locked` に変更する
- `POETRY_*` env / build arg を削除または `UV_*` に置換する
- Docker 起動 import path を確認する
  - 互換導線: `app.main:app`
  - 新標準導線: `koiki_ref_app.asgi:app`

検証:

- `docker build`
- `docker compose build app`
- 可能なら `docker compose up` による起動確認

完了条件:

- Docker build が `poetry.lock` なしで成立する
- Dockerfile 内の install / lockfile 参照が `uv` 前提になっている

実施結果:

- `Dockerfile` と `Dockerfile.unified` を Poetry から `uv` ベースへ更新した
  - `COPY poetry.lock` を `COPY uv.lock` に変更
  - Poetry install step を削除
  - `uv` binary を `ghcr.io/astral-sh/uv:0.11.7` からコピー
  - `uv sync --locked` で `/app/.venv` を作成
  - runtime `PATH` に `/app/.venv/bin` を追加
- Docker / Compose の backend 起動 import path を `koiki_ref_app.asgi:app` に更新した
- `scripts/verify-dockerfile-unified.sh` と `scripts/verify-dockerfile-unified.ps1` から `POETRY_MAX_WORKERS` build arg を削除した
- BuildKit cache mount 中の `uv cache clean` が `Resource busy` で失敗したため、Dockerfile 内から削除した

検証結果:

- `docker build --file Dockerfile.unified --target dev --tag koiki-pyfw-app:dev-uv-check .`
  - 成功
- `docker build --file Dockerfile.unified --target production --build-arg INSTALL_MAIN_ONLY=true --tag koiki-pyfw-app:prod-uv-check .`
  - 成功
- `docker build --file Dockerfile --target production --tag koiki-pyfw-app:prod-dockerfile-uv-check .`
  - 成功
- `docker run --rm --entrypoint python koiki-pyfw-app:prod-uv-check -c "import libkoiki, koiki_ref_app; print(libkoiki.__name__, koiki_ref_app.__name__)"`
  - 成功
- `docker run --rm --entrypoint sh koiki-pyfw-app:prod-uv-check -c "which python && which uvicorn && which alembic"`
  - 成功
  - `/app/.venv/bin/python`
  - `/app/.venv/bin/uvicorn`
  - `/app/.venv/bin/alembic`
- `docker compose -f docker-compose.yml build app`
  - 成功
- `docker compose -f docker-compose.unified.yml --profile dev build app-dev`
  - 成功

状態:

- `完了`

### FU-2: `components/libkoiki` package metadata 整理

優先度:

- 高

対象:

- `components/libkoiki/pyproject.toml`

作業:

- `[tool.poetry]` を削除する
- `[tool.poetry.group.*]` を削除する
- `[tool.poetry.dependencies]` を削除する
- `poetry-core` build backend を削除する
- `setuptools` または別の PEP 517 backend に置き換える
- `src` layout の package discovery を明示する

注意:

- `components/libkoiki/pyproject.toml` 自体は削除しない
- workspace member として `libkoiki` を install / build するために必要である

検証:

- `uv lock --check`
- `uv sync --locked --group dev --group test`
- `uv run python -c "import libkoiki"`

完了条件:

- `components/libkoiki/pyproject.toml` から Poetry 固有設定が消えている
- `libkoiki` が workspace package として import できる

実施結果:

- `components/libkoiki/pyproject.toml` から Poetry 固有設定を削除した
  - `[tool.poetry]`
  - `[tool.poetry.group.dev]`
  - `[tool.poetry.group.test]`
  - `[tool.poetry.dependencies]`
  - `poetry-core` build backend
- build backend を `setuptools.build_meta` に変更した
- `src` layout の package discovery を `[tool.setuptools.packages.find]` で明示した
- component の `[project]` metadata と optional `dev` dependencies は維持した

検証結果:

- `uv lock --check`
  - 成功
- `uv sync --locked --group dev --group test`
  - 成功
  - `libkoiki` が `setuptools.build_meta` で build / reinstall された
- `uv run --locked python -c "import libkoiki; print(libkoiki.__name__)"`
  - 成功
- `DEBUG=true uv run --locked pytest components/libkoiki/tests --collect-only`
  - 成功
  - 115 tests collected

補足:

- `DEBUG` 未上書きの場合、Codex 実行プロセス環境に継承されていた `DEBUG=release` により `Settings.DEBUG` の bool parse error が発生する。
- repository の `.env` には `DEBUG=release` は存在しない。
- これは FU-2 の package metadata 変更とは別件の実行環境課題であり、Codex 側の検証では `DEBUG=true` を明示して実行する。

状態:

- `完了`

### FU-3: Security audit 導線の `uv` 化

優先度:

- 高

対象:

- `SECURITY_AUDIT_COMMANDS.md`
- 必要に応じて security audit 用 workflow

作業:

- `poetry install --with security` を `uv sync --locked --group security` に置換する
- `poetry run pip-audit` を `uv run --locked pip-audit` に置換する
- `poetry run bandit` を `uv run --locked bandit` に置換する
- `bandit` の対象を実コードディレクトリに絞る
  - `app`
  - `components/libkoiki/src`
  - `components/koiki_ref_app/src`
- `poetry show --outdated` / `poetry show --tree` を `uv` の診断コマンドに置換する
  - 例: `uv tree`
  - 必要に応じて `uv pip list`
- `safety` の扱いを決める
  - 現在の標準 security group には `safety` を含めない
  - 原則は `pip-audit` と `bandit` を標準とする
  - `safety` は削除ではなく標準外として明記する
  - `safety` を再採用する場合は dependency group / workflow / license / 依存競合を再評価する
- SBOM は現時点の標準成果物に含めない
  - 必要になった段階で `uv export --format cyclonedx1.5` の preview 扱いを確認する
- CI 例を `astral-sh/setup-uv` と `uv sync --locked --group security` 前提に更新する
- GitHub Dependabot / Dependency Review を推奨導線として別セクション化する
- セキュリティ修正時の dependency update 手順を `uv add` / `uv lock` / `uv sync` 前提に更新する

検証:

- `uv sync --locked --group security`
- `uv run --locked pip-audit`
- `uv run --locked bandit -r app components/libkoiki/src components/koiki_ref_app/src`

完了条件:

- `SECURITY_AUDIT_COMMANDS.md` の実行可能コマンドが `uv` 前提になっている
- security group の実依存と文書が一致している

実施方針:

- 依存性脆弱性監査は `pip-audit` を標準とする
- 静的セキュリティ解析は `bandit` を標準とし、対象は実コードディレクトリに限定する
- `safety` は標準外として明記する
- SBOM は現時点の標準成果物に含めない
- CI 例は `astral-sh/setup-uv` と `uv sync --locked --group security` 前提にする
- GitHub Dependabot / Dependency Review は推奨導線として別セクションに分ける

状態:

- `完了`

実施結果:

- `SECURITY_AUDIT_COMMANDS.md` を `uv` 前提の security audit runbook に更新した
- `safety` は削除ではなく標準外として明記した
- `pip-audit` を依存性脆弱性監査の標準にした
- `bandit` は実コードディレクトリに対象を限定した
  - `app`
  - `components/libkoiki/src`
  - `components/koiki_ref_app/src`
- CI 例を `astral-sh/setup-uv@v6` と `uv sync --locked --group security` 前提に更新した
- GitHub Dependabot / Dependency Review を推奨導線として別セクション化した
- SBOM は現時点の標準成果物に含めない方針を明記した

検証結果:

- `uv sync --locked --group security`
  - 成功
  - `VIRTUAL_ENV=libkoiki\.venv` と project environment `.venv` の不一致 warning は出たが、`uv` は root `.venv` を対象にしており、今回の検証では問題なし
- `uv run --locked pip-audit`
  - 実行成功
  - 5 known vulnerabilities in 4 packages を検出
  - local workspace package の `koiki-ref-app` と `libkoiki` は PyPI 上に存在しないため audit skip された
- `uv run --locked pip-audit --format=json --output=audit-report.json`
  - JSON report 生成成功
- `uv run --locked bandit -r app components/libkoiki/src components/koiki_ref_app/src`
  - 実行成功
  - Total issues:
    - Low: 13
    - Medium: 4
    - High: 0
- `uv run --locked bandit -r app components/libkoiki/src components/koiki_ref_app/src -f json -o security-report.json`
  - JSON report 生成成功
- 生成された `audit-report.json` と `security-report.json` は一時生成物として削除済み
- `.gitignore` への audit report pattern 追加は現時点では保留

検出事項:

- `pip-audit`
  - `pip 26.0.1`: `CVE-2026-3219`
  - `pyjwt 2.11.0`: `CVE-2026-32597`, fix version `2.12.0`
  - `pytest 8.4.2`: `CVE-2025-71176`, fix version `9.0.3`
  - `starlette 0.46.2`: `CVE-2025-54121`, fix version `0.47.2`
  - `starlette 0.46.2`: `CVE-2025-62727`, fix version `0.49.1`
- `bandit`
  - `token_type="bearer"` の `B106`
    - OAuth2 token type 定数であり、false positive として扱う候補
  - `security_logger.py` の `password_reset...` 等の `B105`
    - security event name 定数であり、false positive として扱う候補
  - `logging.py` の `B110`
    - 低リスクだが、将来的に明示的な fallback 処理へ整理する候補
  - `saml_metadata_loader.py` の `xml.etree.ElementTree` / `ET.fromstring`
    - 外部 SAML metadata XML を扱うため、`defusedxml` 等への置換を別 follow-up で検討する

別 follow-up:

- `pip-audit` で検出された依存脆弱性の修正
- `bandit` false positive の抑制方針整理
- `saml_metadata_loader.py` の XML parser hardening
- security audit report 生成物の `.gitignore` 対象化要否確認

### FU-4: Test / auth / SAML runbook の `uv` 化

優先度:

- 中

対象:

- `認証系APIテストガイド.md`
- `docs/saml/SAML_SETUP.md`
- `docs/saml/SAML_MIGRATION_GUIDE.md`
- `docs/agent/skills/testing-plan.md`
- 必要に応じて `docs/backend-audit/*.md`

作業:

- `poetry run pytest` を `uv run pytest` に置換する
- `poetry install` を `uv sync` に置換する
- 古い import path や test path が残っていれば現構成へ更新する
- 過去計画文書として残すべきものは履歴文書扱いに分類する

検証:

- 代表的な test command の collect-only
- `uv run python -c "import libkoiki, koiki_ref_app"`

完了条件:

- 現行 runbook と guide が Poetry を標準手順として提示していない

実施結果:

- `認証系APIテストガイド.md` の test install / pytest command を `uv` 前提へ更新した
  - `poetry install --with test` を `uv sync --locked --group test` に変更
  - `poetry run pytest` を `uv run --locked pytest` に変更
  - 存在しない旧 `tests/unit/test_simple_auth.py` を `components/libkoiki/tests/unit/libkoiki/services/test_simple_auth.py` に変更
- `docs/saml/SAML_SETUP.md` を `uv` 前提へ更新した
  - `poetry install` を `uv sync --locked` に変更
  - `poetry run uvicorn app.main:app` を `uv run --locked uvicorn koiki_ref_app.asgi:app` に変更
  - `alembic upgrade head` を `uv run --locked alembic upgrade head` に変更
- `docs/saml/SAML_MIGRATION_GUIDE.md` の dependency sync 手順を `uv lock && uv sync --locked` に変更した
- `docs/agent/skills/testing-plan.md` の test / smoke command を `uv run --locked` に変更した
- `docs/backend-audit/LOG-09_LOGGING_OPERATIONS_RUNBOOK_ja.md` の運用 test command を `uv run --locked` と現行 component path に変更した
- `docs/backend-audit/LOG-04` から `LOG-11` の作業計画文書に残る Poetry command は、過去作業計画の記録として履歴文書扱いにした

検証結果:

- `DEBUG=true UV_CACHE_DIR=.uv-cache-codex uv run --locked pytest components/libkoiki/tests/unit/libkoiki/services/test_simple_auth.py tests/unit/test_hello.py components/libkoiki/tests/unit/libkoiki/services/test_user_service_simple.py components/koiki_ref_app/tests/integration/app/api/test_todos_api.py --collect-only`
  - 成功
  - 10 tests collected
- `DEBUG=true UV_CACHE_DIR=.uv-cache-codex uv run --locked pytest tests/unit/agent_guidance/ --collect-only`
  - 成功
  - 13 tests collected
- `DEBUG=true UV_CACHE_DIR=.uv-cache-codex uv run --locked python -c "import libkoiki, koiki_ref_app; print(libkoiki.__name__, koiki_ref_app.__name__)"`
  - 成功
- `DEBUG=true UV_CACHE_DIR=.uv-cache-codex uv run --locked python -c "from koiki_ref_app.asgi import app; print(app.title)"`
  - 成功
  - `KOIKI Framework`
- `.\scripts\run-db-integration-tests.ps1 -DbContainerName osskk_postgres_unified`
  - 成功
  - unified PostgreSQL container `osskk_postgres_unified` 上の `test_db` / `test_user` を使用
  - `RUN_DB_INTEGRATION=1`
  - `DATABASE_URL=postgresql+asyncpg://test_user:test_pass@localhost:5432/test_db`
  - 37 passed
    - `components/koiki_ref_app/tests/integration/app/api/test_auth_api.py`: 11 passed
    - `tests/integration/services/test_auth_service_db.py`: 6 passed
    - `tests/integration/services/test_login_security_service_db.py`: 9 passed
    - `tests/integration/services/test_user_service_db.py`: 11 passed
  - warning は既知 follow-up 候補
    - active venv と root `.venv` の不一致 warning
    - `LoginAttemptModel` の SQLAlchemy column warning
    - Pydantic v1 validator / `.dict()` deprecation warning
    - `.env.ci` の `JWT_SECRET` が 32 bytes 未満であることによる PyJWT HMAC key length warning
- Codex 実行環境の user cache 権限差を避けるため一時的に `.uv-cache-codex` を使用した
  - 検証後に削除済み

状態:

- `完了`

### FU-5: 旧 Poetry artifacts / helper の整理

優先度:

- 中

対象:

- `run_make_pip_require.ps1`
- `libkoiki/poetry.lock`
- 旧 `libkoiki/` 配下の残存物
- `requirements.txt`

作業:

- `poetry export` helper を削除または `uv export` ベースへ置換する
- 旧 `libkoiki/poetry.lock` の削除可否を判断する
- `requirements.txt` を維持するか、生成物として廃止するか決める

検証:

- `rg -i "poetry|poetry.lock|poetry-core"` で実運用系の残存を確認する
- `uv lock --check`
- `uv sync --locked`

完了条件:

- 実行され得る helper が Poetry CLI を要求しない
- 旧 lockfile の扱いが明確である

実施結果:

- `run_make_pip_require.ps1` を削除した
  - `poetry export` による root `requirements.txt` 生成 helper だった
  - 現行の標準依存同期は `uv sync` / `uv.lock` であり、helper は不要
- root `requirements.txt` を削除した
  - Poetry export 由来の生成物であり、現行 lockfile 正本ではない
  - 旧 `libkoiki/` layout への editable 参照を含んでいた
- `libkoiki/poetry.lock` を削除した
  - 旧 root-level `libkoiki/` 配下の Poetry lockfile であり、現行 workspace 経路外
- `libkoiki/setup.py` と旧 `libkoiki/` 本体は削除しない
  - 依存管理 follow-up の範囲外とし、旧 layout 整理の別判断に回す
- `ops/requirements.txt` は削除しない
  - ops 配下の別ファイルであり、現在の実装では不要で空にする旨が明記されている

検証結果:

- `rg -n "run_make_pip_require\.ps1|requirements\.txt|libkoiki/poetry\.lock|libkoiki\\poetry\.lock|pip install -r requirements\.txt|poetry export" --glob '!**/.git/**' --glob '!**/.venv/**' .`
  - 実運用 helper と root dependency artifact への参照が残っていないことを確認
  - 残存は `ops/requirements.txt`、履歴文書、follow-up 計画文書のみ

状態:

- `完了`

### FU-6: 履歴文書の扱い明確化

優先度:

- 低

対象:

- `KOIKI-FW_0.6.0.md`
- `KOIKI-FW_0.6.1.md`
- `docs/design_kkfw_0.6.0.md`
- `docs/dev/v0.7-directory-reorganization-plan*.md`
- `docs/dev/v0.7-directory-reorganization-tasks.ja.md`
- `docs/dev/v0.7-task-instructions/*.md`

方針:

- 原則として履歴文書の Poetry 記述は書き換えない
- 読者が現行手順と誤認しやすい場合だけ注記を追加する
- 注記では現行手順として次を案内する
  - `docs/dev/local_setup.md`
  - `docs/dev/test-guide.md`
  - `uv.lock`
  - `uv sync`
  - `uv run`

完了条件:

- 履歴文書と現行 runbook の区別が説明できる
- 過去記録を壊さず、現行手順への導線がある

## 推奨実行順

1. FU-1: Docker / Compose の `uv` 化
2. FU-2: `components/libkoiki` package metadata 整理
3. FU-3: Security audit 導線の `uv` 化
4. FU-4: Test / auth / SAML runbook の `uv` 化
5. FU-5: 旧 Poetry artifacts / helper の整理
6. FU-6: 履歴文書の扱い明確化

## 完了後の確認コマンド

```bash
uv lock --check
uv sync --locked --group dev --group test
uv run pytest --collect-only
uv run python -c "import libkoiki, koiki_ref_app"
```

Poetry 残存確認:

```bash
rg -i "poetry|poetry.lock|poetry-core"
```

この検索結果はゼロである必要はない。
履歴文書に残る記述は許容する。
ただし、実運用系、CI、Docker、runbook、helper script に残る Poetry 前提は follow-up 対象として扱う。
