# KOIKI-FW v0.7 existing docs cleanup follow-up plan

## 目的

Stage 2 の `uv` 実移行と follow-up により、依存管理、Docker、CI、security audit、主要 runbook の標準経路は `uv` に切り替わった。

次に、実行時 warning や OSS 依存脆弱性の修正へ進む前に、既存 Markdown / docs / agent guidance / tool history に残る古い前提を整理する。

この文書は、不要または齟齬のある文書・履歴・agent/tool 由来ファイルを段階的に整理するための follow-up 計画である。

## 現在の基準

現行の標準経路:

- 依存同期: `uv sync --locked`
- 実行: `uv run --locked ...`
- lockfile 正本: `uv.lock`
- reusable framework 正本: `components/libkoiki`
- reference app 正本: `components/koiki_ref_app`
- application ASGI 正本: `koiki_ref_app.asgi:app`
- root `app/`: 互換 wrapper

現行文書として残すべきではない前提:

- Poetry を標準依存管理として扱う記述
- root `requirements.txt` を標準依存管理として扱う記述
- 旧 root `libkoiki/` を正本として扱う記述
- root `app/` を実装正本として扱う記述
- root `alembic/` を migration 正本として扱う記述
- `app.main:app` を新規推奨 ASGI path として扱う記述
- 生成 cache / tool-local state を Git 保管対象として扱うこと

## 分類ルール

### A. 現行運用文書

README、setup、Docker、security audit、test guide、SAML guide、agent guidance など、開発者や agent が現在参照し得る文書。

原則として現行構成へ更新する。

### B. 履歴・計画文書

release note、過去 design、v0.7 task instruction、backend audit work plan など、過去判断や作業経緯を記録する文書。

原則として内容を書き換えず、必要に応じて archive / historical reference として扱う。

### C. agent / tool 由来ファイル

`.serena/`、`.kiro/`、`.specstory/`、`.claude/`、`.github/instructions/` など。

現在の agent 作業に影響するものは更新する。生成 cache や会話履歴は Git 管理の要否を判断する。

## Docs Cleanup FU-1: tracked agent/tool history の整理

対象:

- `.serena/cache/**`
- `.serena/memories/*.md`
- `.serena/project.yml`
- `.kiro/steering/*.md`
- `.specstory/history/**`
- `.specstory/cli/**`

目的:

- tool-generated cache と共有すべき知識を分離する
- 現行構成と矛盾する agent/tool history が作業標準として読まれないようにする
- Git 管理対象に残すものとローカル生成物に戻すものを明確にする

推奨方針:

- `.serena/cache/**`
  - 再生成可能な cache として Git 管理から外す
  - `.serena/.gitignore` の `/cache` は維持する
- `.serena/project.yml`
  - Serena project 設定として Git 管理を継続してよい
- `.serena/memories/*.md`
  - 残す場合は現行 `uv` / `components` 構成へ更新する
  - 共有不要なら削除候補とする
- `.kiro/steering/*.md`
  - 現在も使うなら現行構成へ更新する
  - 使わないなら削除または archive 候補とする
- `.specstory/history/**`
  - 会話履歴として残す価値はあるが、現行 docs としては危険
  - 原則として Git 管理から外すか、archive / historical 扱いを明確にする
- `.specstory/cli/**`
  - tool 設定として必要か確認する
  - project-local 設定として共有する価値がなければ Git 管理対象から外す

完了条件:

- tracked 生成 cache の扱いが決まっている
- `.serena/memories` を残す場合、Poetry / 旧 path 前提が現行構成へ更新されている
- `.kiro/steering` を残す場合、Poetry / 旧 path 前提が現行構成へ更新されている
- `.specstory/history` を残す場合、現行 docs ではなく履歴であることが明確になっている
- Git 管理対象に残す agent/tool 由来ファイルを説明できる

検証コマンド:

```powershell
git ls-files .serena .kiro .specstory
rg -n -i "poetry|poetry\.lock|requirements\.txt|app\.main:app|libkoiki/|alembic/" .serena .kiro .specstory --glob "*.md"
git status --short
```

## Docs Cleanup FU-2: agent guidance の現行 path 修正

対象:

- `AGENTS.md`
- `CLAUDE.md`
- `.claude/settings.json`
- `.claude/skills/**`
- `.github/copilot-instructions.md`
- `.github/instructions/*.md`
- `docs/agent/*.md`
- `docs/agent/skills/**`
- `components/libkoiki/AGENTS.md`
- `components/libkoiki/CLAUDE.md`
- `components/koiki_ref_app/AGENTS.md`
- `components/koiki_ref_app/CLAUDE.md`

目的:

- Codex / Claude / Copilot / local skills が旧 path を正本として扱わないようにする
- `components/libkoiki` と `components/koiki_ref_app` を現行正本として説明する
- root `app/` と `app.main:app` は互換導線として限定的に扱う

主な修正方針:

- `libkoiki/` 正本表現を `components/libkoiki` へ更新する
- `app/` 正本表現を `components/koiki_ref_app` へ更新する
- `app/main.py` は compatibility wrapper として説明する
- SSO / SAML は `components/koiki_ref_app/src/koiki_ref_app` 側の責務として説明する
- test path は `components/libkoiki/tests` と `components/koiki_ref_app/tests` を標準とする
- `.claude/settings.json` の Poetry 許可が現行標準に合うか判断する

完了条件:

- agent guidance が現行ディレクトリ構成を標準として説明している
- root `app/` は互換 wrapper としてのみ説明されている
- Poetry を標準コマンドとして許可・推奨する agent 設定が残っていない
- agent が新規作業で旧 path へ誘導されない

検証コマンド:

```powershell
rg -n -i "poetry|poetry\.lock|poetry run" AGENTS.md CLAUDE.md .claude .github docs/agent components/*/AGENTS.md components/*/CLAUDE.md
rg -n "libkoiki/|app/|app\.main:app|tests/unit/libkoiki|tests/unit/app" AGENTS.md CLAUDE.md .claude .github docs/agent components/*/AGENTS.md components/*/CLAUDE.md
```

## Docs Cleanup FU-3: 現行 runbook / guide の軽微な齟齬修正

対象候補:

- `ops/README.md`
- `DOCKER_SETUP.md`
- `README-deploy.md`
- `ENTERPRISE_DEPENDENCY_STRATEGY.md`
- その他 root / docs 配下の現行運用 guide

目的:

- 現行運用 guide に残る小さな齟齬を解消する
- 履歴資料と現行 runbook の境界をさらに明確にする

既知候補:

- `ops/README.md`
  - `ops/requirements.txt` は外部依存なしの placeholder であり、標準依存定義ではない
- `DOCKER_SETUP.md`
  - 旧 Docker / Compose ファイルは「削除予定」ではなく、現時点では削除済みとして扱う
- `README-deploy.md`
  - 現行 deploy guide として維持するか、履歴注記付き資料として残すかを判断する
- `ENTERPRISE_DEPENDENCY_STRATEGY.md`
  - historical reference として残すか、archive 側へ寄せるかを判断する

完了条件:

- 現行 runbook が古いファイルや旧依存方式を標準として説明していない
- 履歴資料として残す文書にはその旨が明記されている
- 軽微な齟齬が issue / follow-up なしで後続作業を妨げない

検証コマンド:

```powershell
rg -n -i "poetry|requirements\.txt|削除予定|app\.main:app|libkoiki/|alembic/" README.md README-deploy.md DOCKER_SETUP.md SECURITY_AUDIT_COMMANDS.md ops/README.md docs
```

## Docs Cleanup FU-4: 履歴文書 / 現行文書の配置整理

対象候補:

- `KOIKI-FW_0.6.0.md`
- `KOIKI-FW_0.6.1.md`
- `docs/design_kkfw_0.6.0.md`
- `docs/dev/v0.7-task-instructions/**`
- `docs/backend-audit/LOG-*WORK_PLAN*.md`
- `docs/backend-audit/SESSION_WORK_LOG_*.md`

目的:

- 履歴文書を削除せず、現行文書と誤読されにくい配置へ整理する
- README / docs index から、現行文書と履歴資料の導線を分ける

選択肢:

- 現在位置のまま historical note を追加する
- `docs/archive/` または `docs/dev/history/` へ移動する
- versioned release note として root に残す

完了条件:

- 履歴文書が現行手順として読まれない
- 現行 setup / test / Docker / security / SAML docs への導線が明確である
- 過去判断を参照する必要がある場合に辿れる

検証コマンド:

```powershell
rg -n -i "historical|履歴|archive|現行|標準経路" README.md docs
rg -n -i "poetry|app\.main:app|libkoiki/|alembic/" KOIKI-FW_0.6.0.md KOIKI-FW_0.6.1.md docs/design_kkfw_0.6.0.md docs/dev/v0.7-task-instructions docs/backend-audit
```

## Docs Cleanup FU-5: 最終横断確認

目的:

- 文書整理後に、現行標準と履歴資料の境界が説明できる状態にする
- warning / vulnerability 修正へ進む前に、参照文書が作業を誤誘導しないことを確認する

確認観点:

- Poetry 記述の残存分類
- old path 記述の残存分類
- Git 管理対象の生成物
- agent guidance の正確性
- setup / Docker / CI / security / test / SAML の標準経路

完了条件:

- `uv` / `components` 構成が現行標準として一貫している
- 残る旧記述は履歴・互換・archive として説明できる
- Git 管理対象に残る agent/tool 由来ファイルの理由を説明できる
- 次工程として warning / OSS vulnerability 修正へ進める

検証コマンド:

```powershell
git status --short
git ls-files .serena .kiro .specstory .claude .github docs README.md README-deploy.md DOCKER_SETUP.md ops/README.md
rg -n -i "poetry|poetry\.lock|poetry-core|python-poetry|requirements\.txt|pip install -r" --glob "!**/.git/**" --glob "!**/.venv/**" .
rg -n "app\.main:app|uvicorn app\.main|libkoiki/|app/|alembic/|tests/unit/libkoiki|tests/unit/app" --glob "!**/.git/**" --glob "!**/.venv/**" .
```

## 推奨実行順

1. FU-1: tracked agent/tool history の整理
2. FU-2: agent guidance の現行 path 修正
3. FU-3: 現行 runbook / guide の軽微な齟齬修正
4. FU-4: 履歴文書 / 現行文書の配置整理
5. FU-5: 最終横断確認

## この後の後続候補

Docs Cleanup 完了後に扱う候補:

- `pip-audit` で検出済みの OSS 依存脆弱性修正
- SAML metadata XML parser の `defusedxml` 化
- Bandit low findings の誤検知整理
- DB integration 実行時 warning の改善
- active venv と root `.venv` の運用差異整理
