# v0.7.0 final release check results

作成日: 2026-05-05

計画文書:

- `docs/dev/v0.7.0-final-release-check-plan.ja.md`

## 1. 対象

release final check の実行結果を記録する。

この記録は、tag / GitHub release 作成前の判断材料として扱う。最終的な release note に転記すべき内容があれば、`docs/releases/KOIKI-FW_0.7.0.md` の Validation 欄へ反映する。

## 2. 現在の結果サマリ

| ID | 概要 | 状態 |
| --- | --- | --- |
| `REL-01` | checkout / branch hygiene / diff check | 完了 |
| `REL-02` | package / FastAPI / `/health` version check | 完了 |
| `REL-03` | Todo API sample router 維持判断の再確認 | 完了 |
| `REL-04` | lock / agent guidance / collect-only / unit validation | 完了 |
| `REL-05` | container build / run / UI smoke / log review | 完了 |
| `REL-06` | README / design / release note final review | 完了 |
| `REL-07` | pip-audit / Bandit / known warning review | 完了 |
| `REL-08` | validation evidence / release blocker 判定記録 | 完了 |
| `REL-09` | tag / GitHub release 作成判断 | 完了 |
| `REL-10` | clean clone / e2e / DB integration / CI rerun | 任意 / 未実施 |

## 3. REL-01 checkout / branch hygiene

実行日: 2026-05-05

対象 branch:

```text
topic/v070-final-check-plan
```

対象 commit:

```text
45374f4 Merge pull request #89 from zaziedlm/topic/dm12c-app-main-compat-policy
```

確認コマンド:

```powershell
git status --short --branch
git log --oneline --decorate -8
git diff --check
git diff --stat
```

結果:

- `topic/v070-final-check-plan` は `origin/dev/v0.7` / `dev/v0.7` と同じ `45374f4` から開始している
- 作業ツリーの差分は release final check 計画文書と deferred maintenance task 追記のみ
- `git diff --check` は成功
- release tag は topic branch では切らず、final check plan を反映した後の release 対象 commit から作成する

判定:

```text
REL-01: pass
```

備考:

- この時点の作業ツリーは clean ではないが、理由は final check plan / results 作成中の docs 差分である
- release 判定そのものを実行する前には、計画差分を commit / merge したうえで clean working tree を再確認する

## 4. REL-02 package / FastAPI / runtime metadata

実行日: 2026-05-05

確認コマンド:

```powershell
uv lock --check
$env:DEBUG='True'; uv run --locked python -c "import libkoiki; print(libkoiki.__version__)"
$env:DEBUG='True'; uv run --locked python -c "from koiki_ref_app.asgi import app; print(app.version)"
```

結果:

```text
uv lock --check
  Resolved 87 packages in 1ms

libkoiki.__version__
  0.7.0

koiki_ref_app.asgi:app.version
  0.7.0
```

判定:

```text
REL-02 local metadata: pass
```

備考:

- Codex sandbox ではユーザー側 uv cache へアクセスできず、初回 `uv` 実行は `Failed to initialize cache` で失敗した
- repository / lock file の問題ではないため、承認付き実行で再確認した
- `koiki_ref_app.asgi` import 時に development middleware の INFO log が出力されるが、version 確認自体は成功している
- container 起動後の `/health` / `/` response は `REL-05` で確認した

## 5. REL-03 Todo API sample router 維持判断

実行日: 2026-05-05

確認コマンド:

```powershell
rg -n "todo|todos|Todo" components/libkoiki/src components/koiki_ref_app/src components/libkoiki/tests components/koiki_ref_app/tests docs/agent README.md docs/design_kkfw_0.7.0.md docs/releases/KOIKI-FW_0.7.0.md -S
rg --files components/libkoiki/src components/koiki_ref_app/src | rg "todo|router|dependencies|app_factory|asgi"
rg -n "starter/sample|sample / starter|starter capability|sample router|business API|apps/|Todo" docs/agent README.md docs/design_kkfw_0.7.0.md docs/releases/KOIKI-FW_0.7.0.md docs/dev/dm14-api-ownership-boundary-policy.ja.md apps/README.md -S
rg -n "libkoiki.*api_router|include_router|ToDos Sample|todos\.router" components/koiki_ref_app/src/koiki_ref_app/app_factory.py components/libkoiki/src/libkoiki/api/v1/router.py components/koiki_ref_app/src/koiki_ref_app/api/v1/router.py
```

確認結果:

- Todo endpoint / model / schema / repository / service は `components/libkoiki/src/libkoiki/` 側に存在する
- `components/libkoiki/src/libkoiki/api/v1/router.py` で `todos.router` が `prefix="/todos"`、`tags=["ToDos Sample"]` として include されている
- `components/koiki_ref_app/src/koiki_ref_app/app_factory.py` は app 固有 router に加えて `libkoiki.api.v1.router.api_router` を include している
- `components/koiki_ref_app/src/koiki_ref_app/api/v1/router.py` は SSO / SAML / business clock など app 固有 API を所有しており、Todo の concrete endpoint は持っていない
- `docs/agent/boundaries.md`、`docs/agent/libkoiki.md`、`docs/agent/app.md`、agent skill docs は、Todo を `libkoiki` の starter/sample capability として扱い、新規 business API の配置前例にしない方針を説明している
- `docs/design_kkfw_0.7.0.md`、`docs/releases/KOIKI-FW_0.7.0.md`、`docs/dev/dm14-api-ownership-boundary-policy.ja.md` でも同じ方針が説明されている
- `apps/README.md` は downstream / customer-specific backend API の配置先として `apps/` を説明しており、Todo sample の維持判断と矛盾しない

判定:

```text
REL-03: pass
```

v0.7.0 判断:

- Todo API は `libkoiki` sample router / starter capability として明示維持する
- v0.7.0 直前に app layer 移動、optional router 化、generic base capability 化は行わない
- Todo が `libkoiki` にあることは、新規 business API を `components/libkoiki/` に置く前例ではない

## 6. REL-04 automated validation

実行日: 2026-05-05

確認コマンド:

```powershell
$env:DEBUG='True'; uv run --locked pytest tests/unit/agent_guidance
$env:DEBUG='True'; uv run --locked pytest --collect-only components/libkoiki/tests tests/unit/agent_guidance components/koiki_ref_app/tests tests/integration/services -m "not db_integration"
$env:DEBUG='True'; uv run --locked pytest components/libkoiki/tests components/koiki_ref_app/tests -m "not db_integration"
```

結果:

```text
uv run --locked pytest tests/unit/agent_guidance
  17 passed in 1.09s

uv run --locked pytest --collect-only components/libkoiki/tests tests/unit/agent_guidance components/koiki_ref_app/tests tests/integration/services -m "not db_integration"
  222/259 tests collected, 37 deselected

uv run --locked pytest components/libkoiki/tests components/koiki_ref_app/tests -m "not db_integration"
  204 passed, 1 skipped, 11 deselected, 33 warnings in 3.90s
```

warning:

- `components/libkoiki/src/libkoiki/core/transaction.py` の transaction mock RuntimeWarning
- FastAPI / Starlette の `HTTP_422_UNPROCESSABLE_ENTITY` deprecation warning

判定:

```text
REL-04: pass
```

備考:

- warning は既存の deferred maintenance 記録と整合する既知 warning
- 今回の release final check plan / Todo sample router 判断による新規 failure はない

## 7. REL-05 container / smoke / log review

実行日: 2026-05-05

確認コマンド:

```powershell
.\start-docker.ps1 unified-prod-build
.\start-docker.ps1 unified-prod
Invoke-RestMethod http://localhost:8000/health | ConvertTo-Json -Depth 5
Invoke-RestMethod http://localhost:8000/ | ConvertTo-Json -Depth 5
docker ps --filter "name=koiki" --filter "name=osskk" --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
docker logs --tail 220 koiki_app_prod_unified
docker logs --tail 160 koiki_frontend_prod_unified
```

container build:

```text
app-prod image: built
frontend-prod image: built
```

build 時の注意:

```text
npm ci
  11 vulnerabilities (5 moderate, 5 high, 1 critical)
```

この npm audit summary は build failure ではないが、`REL-07` の security / dependency audit で release blocker 判定対象にする。

dependency remediation 後の再ビルド:

```text
next: 15.5.15
axios: ^1.16.0

app-prod image: built
frontend-prod image: built
frontend build: Next.js 15.5.15 / Compiled successfully
npm ci audit summary: 9 vulnerabilities (5 moderate, 4 high)
```

この再ビルド後の `npm ci` audit summary は dev dependency を含む。`REL-07` で `npm audit --omit=dev` を再確認し、production dependency の critical / high は 0 件まで解消済み。

container run:

```text
koiki_app_prod_unified: healthy
koiki_frontend_prod_unified: healthy
osskk_postgres_unified: healthy
osskk_keycloak_unified: healthy
```

dependency remediation 後の再起動確認:

```text
koiki_app_prod_unified: Up / healthy
koiki_frontend_prod_unified: Up / healthy
osskk_postgres_unified: Up / healthy
osskk_keycloak_unified: Up / healthy
```

startup warning:

- Docker Compose が orphan containers として `osskk_fastapi_app` / `osskk_nextjs_frontend` を検出した
- 今回の unified prod 起動自体は成功している

API version smoke:

```json
{
  "status": "healthy",
  "service": "koiki-framework",
  "version": "0.7.0"
}
```

dependency remediation 後の再確認でも `/health` と `/` は `version: 0.7.0` を返した。

```json
{
  "service": "KOIKI Framework API",
  "version": "0.7.0",
  "docs": "/docs",
  "health": "/health"
}
```

API login / Todo smoke:

使用ユーザー:

```text
user@example.com / testuser123456
```

確認内容:

- password login: success
- Todo create: success
- Todo update: success
- Todo list: success
- Todo delete: success
- Todo completion toggle: `is_completed=true` で success

代表結果:

```json
{
  "login": true,
  "created_id": 34,
  "list_count": 2,
  "deleted_id": 34
}
```

```json
{
  "id": 35,
  "is_completed": true,
  "owner_id": 5
}
```

dependency remediation 後の再確認:

```json
{
  "login": true,
  "created_id": 36,
  "updated_completed": true,
  "list_count": 2,
  "deleted_id": 36
}
```

log review:

- backend startup: database connection / migration / router include / application startup completed
- frontend startup: Next.js 15.5.15 ready
- login / Todo CRUD は `http.status_code` 200 / 201 / 204 で記録
- Traceback / import error / startup error / unexpected 5xx は確認されていない

known warning:

- `Redis URL not configured. Redis-dependent features ... might be disabled or limited.`
- `InsecureKeyLengthWarning`: JWT HMAC key が 27 bytes で、HS256 推奨 32 bytes 未満

判定:

```text
REL-05 container build/run/API smoke/log review: pass
REL-05 browser UI smoke: pass
```

備考:

- ブラウザ UI 目視の login / Todo 操作はユーザー側で実施済み
- Codex 側では API 経由で login / Todo list / create / update / completion toggle / delete を再確認済み

## 8. REL-06 release docs final review

実行日: 2026-05-05

確認コマンド:

```powershell
rg -n "実施中|対応中|未実施|DM-12-C|release preparation 後|互換終了判断|暫定維持|0\.6\.1|v0\.6\.0" README.md docs/design_kkfw_0.7.0.md docs/releases/KOIKI-FW_0.7.0.md docs/dev/deferred-maintenance-tasks.ja.md docs/dev/dm12-legacy-compatibility-inventory.ja.md docs/dev/dm14-api-ownership-boundary-policy.ja.md docs/agent apps/README.md -S
rg -n "components/libkoiki|components/koiki_ref_app|koiki_ref_app\.asgi:app|app\.main:app|apps/|Todo API|sample / starter|business API" README.md docs/design_kkfw_0.7.0.md docs/releases/KOIKI-FW_0.7.0.md docs/dev/deferred-maintenance-tasks.ja.md docs/dev/dm14-api-ownership-boundary-policy.ja.md docs/agent apps/README.md -S
git diff --check
```

確認結果:

- `README.md`、`docs/design_kkfw_0.7.0.md`、`docs/releases/KOIKI-FW_0.7.0.md` は v0.7.0 の標準導線を `components/libkoiki/`、`components/koiki_ref_app/`、`koiki_ref_app.asgi:app` として説明している
- `app.main:app` は compatibility wrapper として v0.7.0 では互換維持する説明になっている
- Todo API は `libkoiki` の sample / starter capability であり、新規 business API の配置前例ではない説明になっている
- `apps/README.md` は downstream / customer-specific backend API の配置先として `apps/` を説明している
- `docs/design_kkfw_0.7.0.md` に残っていた、DM-12-C を「release preparation 後に扱う」とする古い表現を、v0.7.0 では互換維持判断済みの表現へ更新した
- `docs/releases/KOIKI-FW_0.7.0.md` の Validation に final release check plan / results への導線を追加した
- `git diff --check` は成功

判定:

```text
REL-06: pass
```

備考:

- `docs/dev/dm14-api-ownership-boundary-policy.ja.md` の「暫定維持」は DM-14 作成時点の判断経緯として残る
- `docs/dev/deferred-maintenance-tasks.ja.md` の「暫定対応」は過去 DM-08 の履歴説明であり、v0.7.0 release docs の矛盾ではない

## 9. REL-07 security / dependency audit

実行日: 2026-05-05

確認コマンド:

```powershell
$env:DEBUG='True'; uv run --locked pip-audit
$env:DEBUG='True'; uv run --locked bandit -c pyproject.toml -r components apps tests
$env:DEBUG='True'; uv run --locked bandit -c pyproject.toml -r components/libkoiki/src components/koiki_ref_app/src apps
npm audit --json
npm audit --omit=dev --json
npm install next@15.5.15 axios@^1.15.2
npm install next@15.5.15 --save-exact
npm run build
```

Python dependency audit:

```text
uv run --locked pip-audit
  pip 26.0.1 / CVE-2026-3219
  koiki-ref-app / libkoiki は local package のため PyPI audit 対象外
```

判定:

- `pip 26.0.1 / CVE-2026-3219` は既知残件
- `pip` は audit 実行環境側の package であり、KOIKI-FW runtime dependency としては扱わない
- v0.7.0 release blocker ではない

Bandit:

```text
uv run --locked bandit -c pyproject.toml -r components apps tests
  High: 0
  Medium: 4
  Low: 931
```

```text
uv run --locked bandit -c pyproject.toml -r components/libkoiki/src components/koiki_ref_app/src apps
  No issues identified.
  High: 0
  Medium: 0
  Low: 0
```

判定:

- tests まで含めた scan では pytest `assert` などの test-only Low が大量に出る
- runtime source に絞ると Bandit issue は 0
- v0.7.0 release blocker ではない

Frontend audit 初回:

```text
npm audit --omit=dev --json
  critical: 1
  high: 1
  moderate: 2
  total: 4
```

release blocker:

- `next 15.5.2`: critical / high を含む
- `axios ^1.11.0`: high を含む

対応:

```text
next: 15.5.2 -> 15.5.15
axios: ^1.11.0 -> ^1.16.0
```

対応後確認:

```text
npm run build
  Next.js 15.5.15
  Compiled successfully

npm audit --omit=dev --json
  critical: 0
  high: 0
  moderate: 2
  total: 2
```

残件:

- `next -> postcss` の moderate 2件
- npm audit の fix suggestion は Next 9 系への semver-major downgrade であり、現実的な v0.7.0 直前対応ではない

判定:

```text
REL-07: pass with follow-up
```

v0.7.0 release blocker 判定:

- Python runtime dependency: blocker なし
- Python runtime source Bandit: blocker なし
- frontend prod critical/high: remediation 済み
- frontend prod moderate: follow-up
- frontend dev dependency audit: high を含む残件あり。ただし production-only audit では critical/high 0 のため、v0.7.0 tag blocker ではなく follow-up 扱い

変更:

- `frontend/package.json`
- `frontend/package-lock.json`

## 10. REL-08 validation evidence / release blocker 判定

実行日: 2026-05-05

判定対象:

- version metadata / FastAPI metadata
- Todo API sample router 維持判断
- automated tests
- container build / run / API smoke / UI smoke / log review
- release docs final review
- security / dependency audit

release blocker:

```text
なし
```

v0.7.0 tag 作成前に満たすべき条件:

- この final check plan / results と dependency remediation を commit する
- topic branch を `dev/v0.7` へ merge する
- merge 後の release 対象 commit で `git status` が clean であることを確認する
- release 対象 commit で `git diff --check` 相当の whitespace check が通ることを確認する

v0.7.x follow-up:

- `npm audit --omit=dev` の remaining moderate 2件
- dev dependency を含む `npm audit` の high / moderate 残件
- audit 実行環境側 `pip 26.0.1 / CVE-2026-3219`
- local unified prod の Docker orphan container 警告整理
- production container の JWT HMAC key length warning 解消
- Redis URL 未設定時の production profile 表示方針整理
- tests まで含めた Bandit scan の test-only `assert` noise 整理

判定:

```text
REL-08: pass
```

備考:

- frontend production dependency の critical / high は `next 15.5.15`、`axios ^1.16.0` への更新で解消済み
- Todo API は v0.7.0 では `libkoiki` sample router / starter capability として維持し、release blocker にはしない
- `app.main:app` compatibility wrapper は v0.7.0 で維持するため、tag 前の削除判断は不要

## 11. REL-09 tag / GitHub release 作成判断

実行日: 2026-05-05

確認コマンド:

```powershell
git status --short --branch
git log --oneline --decorate -5
git tag --list "v0.7.0"
```

確認結果:

```text
current branch: topic/v070-final-check-plan
current base: 45374f4 Merge pull request #89 from zaziedlm/topic/dm12c-app-main-compat-policy
local tag v0.7.0: not found
```

作業ツリー:

- final check plan / results 作成中の docs 差分あり
- release docs final review による docs 差分あり
- security remediation による `frontend/package.json` / `frontend/package-lock.json` 差分あり

判断:

```text
REL-09: tag / GitHub release 作成は未実行
REL-09: tag 作成前条件の整理は pass
```

tag 作成へ進める条件:

1. `topic/v070-final-check-plan` の差分を review し、commit する
2. PR を作成し、`dev/v0.7` へ merge する
3. merge 後の `dev/v0.7` を fresh pull し、`git status --short --branch` が clean であることを確認する
4. release 対象 commit で必要最小限の validation を再実行する
5. `v0.7.0` tag / GitHub release は、topic branch ではなく release 対象 commit から作成する

備考:

- 現時点で `v0.7.0` local tag は存在しない
- release blocker は `REL-08` 時点でなし
- v0.7.x follow-up は tag blocker から分離して扱う

## 12. optional / follow-up tasks

実行日: 2026-05-05

この章は、v0.7.0 final check で見えた残件を release blocker から分離し、v0.7.x 以降の再確認タスクとして整理する。

### 12.1 `FUP-01` frontend production audit moderate 残件

分類: `security/dependency`

検出元:

```powershell
npm audit --omit=dev --json
```

現象:

- production dependency audit は critical / high 0 件まで解消済み
- remaining moderate 2件は `next -> postcss` 系の検出
- npm audit の自動 fix suggestion は Next 9 系への semver-major downgrade であり、v0.7.0 直前対応としては採用しない

v0.7.0 blocker にしない理由:

- production dependency の critical / high は解消済み
- suggested fix が現行 Next 系からの非現実的 downgrade である
- container build / browser smoke / API smoke は成功している

推奨対応:

- Next / postcss の upstream fix または現実的な patch path を v0.7.x で再確認する
- 次回 release 前に `npm audit --omit=dev --json` を再実行する

### 12.2 `FUP-02` frontend dev dependency audit 残件

分類: `security/dependency`

検出元:

```powershell
npm audit --json
```

現象:

- dev dependency を含む audit では high / moderate が残る
- production-only audit の critical / high とは分離して扱う

v0.7.0 blocker にしない理由:

- production runtime の blocker は `npm audit --omit=dev` で判定し、critical / high は解消済み
- dev dependency 残件は release artifact の runtime smoke を止めていない

推奨対応:

- frontend tooling / dev dependency の更新余地を v0.7.x で確認する
- CI の audit policy を production-only と dev-inclusive に分けるか検討する

### 12.3 `FUP-03` audit 実行環境側 `pip` CVE

分類: `security/dependency`

検出元:

```powershell
uv run --locked pip-audit
```

現象:

```text
pip 26.0.1 / CVE-2026-3219
```

v0.7.0 blocker にしない理由:

- `pip` は KOIKI-FW の application runtime dependency ではない
- `pip-audit` 実行環境側の package として検出されている
- `pip-audit` 結果上、該当検出の fix version は空である

推奨対応:

- audit 実行環境の `pip` 更新可否を v0.7.x で再確認する
- CI / local audit 環境の管理方針を整理する

### 12.4 `FUP-04` Docker orphan container 警告

分類: `runtime/profile`

検出元:

```powershell
.\start-docker.ps1 unified-prod
```

現象:

- Docker Compose が `osskk_fastapi_app` / `osskk_nextjs_frontend` を orphan containers として検出する
- unified prod stack 自体は起動し、app / frontend / postgres / keycloak は `healthy`

v0.7.0 blocker にしない理由:

- current unified prod containers は正常起動している
- browser login / Todo smoke は成功している
- 既存 local container の cleanup 表示であり、release artifact の failure ではない

推奨対応:

- `start-docker.ps1` の profile 切替時に orphan cleanup 手順を明記する
- 必要なら `--remove-orphans` 相当を運用手順に含めるか検討する

### 12.5 `FUP-05` JWT HMAC key length warning

分類: `runtime/profile`

検出元:

```text
InsecureKeyLengthWarning: The HMAC key is 27 bytes long, which is below the minimum recommended length of 32 bytes for SHA256.
```

現象:

- production container smoke の login / token decode 時に PyJWT warning が出る
- login / Todo smoke は成功している

v0.7.0 blocker にしない理由:

- 既知 warning として過去 DM-08 / DM-09 / DM-11 の残件と整合している
- v0.7.0 final check では認証 runtime failure は確認されていない

推奨対応:

- local / sample / production profile の `JWT_SECRET` を HS256 推奨 32 bytes 以上に更新する
- secret 更新時に docs / `.env` template / container profile の整合を確認する

### 12.6 `FUP-06` Redis URL 未設定 warning

分類: `runtime/profile`

検出元:

```text
Redis URL not configured. Redis-dependent features (Events, Rate Limiting Strategy, Caching) might be disabled or limited.
```

現象:

- unified prod 起動時に Redis 未設定 warning が出る
- fixed-window rate limiter は initialized として記録される
- login / Todo smoke は成功している

v0.7.0 blocker にしない理由:

- v0.7.0 の必須 smoke 対象である login / Todo / health は Redis なしで正常
- Redis-dependent features の制限を知らせる warning であり、startup failure ではない

推奨対応:

- production profile で Redis を必須にするか、optional warning として維持するかを v0.7.x で整理する
- docs / environment template / runtime log level の整合を確認する

### 12.7 `FUP-07` Bandit test-only noise 整理

分類: `validation/noise`

検出元:

```powershell
uv run --locked bandit -c pyproject.toml -r components apps tests
```

現象:

- tests まで含めると pytest `assert` などの test-only Low が大量に出る
- runtime source に絞った Bandit scan は issue 0

v0.7.0 blocker にしない理由:

- release 対象 runtime source の Bandit issue は 0
- 残件は test code を含む scan policy / noise の問題である

推奨対応:

- release gate 用 Bandit scope と full scan scope を分ける
- test-only finding の baseline / skip 方針を v0.7.x で整理する

### 12.8 `FUP-08` REL-10 optional coverage

分類: `optional/coverage`

対象:

- clean clone 相当の別 directory validation
- browser e2e smoke の自動化
- DB integration full scope
- GitHub Actions 上の PR checks rerun
- runtime agent skill selection smoke の記録

v0.7.0 blocker にしない理由:

- mandatory check は REL-01 から REL-09 まで完了している
- browser UI smoke はユーザー操作で確認済み
- API / container / logs の確認も成功している

推奨対応:

- v0.7.0 tag 直前または v0.7.x で、時間と環境が許す範囲で追加実施する
- 自動化する場合は release gate と exploratory check を分ける

### follow-up summary

v0.7.0 tag blocker:

```text
なし
```

v0.7.x follow-up:

| ID | 分類 | 概要 | blocker |
| --- | --- | --- | --- |
| `FUP-01` | security/dependency | frontend production audit moderate 残件 | no |
| `FUP-02` | security/dependency | frontend dev dependency audit 残件 | no |
| `FUP-03` | security/dependency | audit 実行環境側 `pip` CVE | no |
| `FUP-04` | runtime/profile | Docker orphan container 警告 | no |
| `FUP-05` | runtime/profile | JWT HMAC key length warning | no |
| `FUP-06` | runtime/profile | Redis URL 未設定 warning | no |
| `FUP-07` | validation/noise | Bandit test-only noise 整理 | no |
| `FUP-08` | optional/coverage | REL-10 optional coverage | no |
