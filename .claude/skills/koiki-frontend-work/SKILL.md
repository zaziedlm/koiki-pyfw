---
name: koiki-frontend-work
description: Implement or review root frontend Vite + React SPA work, including UI, routes, feature API clients, Query, browser auth/CSRF integration, configuration, and frontend tests. Use with the owning backend skill for API contract changes.
---

# KOIKI Frontend Work

Use the canonical frontend skill guidance:

@docs/agent/skills/koiki-frontend-work/SKILL.md

Keep these shared rules in view:

- `apps/` is a backend-only business composition layer
- backend authorization and Cookie/CSRF enforcement remain backend-owned
- root `frontend/` is the reference Vite + React SPA
