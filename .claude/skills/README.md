# Claude Skills

This directory exists for Claude Code skill discovery.

These skill folders are thin adapters.
Canonical skill content lives under `docs/agent/skills/`.

When updating a skill:

1. edit the canonical version under `docs/agent/skills/`
2. keep the matching wrapper here minimal and aligned
